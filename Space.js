function Space(height, isInReverse) {
    this.height = height;
    this.splinePoints = 1;
    this.isInReverse = isInReverse;
}

Space.prototype.putInChain = function (array, lengths, pointsOffset) {
    
};